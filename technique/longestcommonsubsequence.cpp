#include <bits/stdc++.h>

using namespace std;
char a[2010],b[2010];
int dp[2010][2010];

int main()
{
    cin.tie(0)->sync_with_stdio(0);
    int i,j,m,n;
    cin >> a+1
    cin >> b+1;
    n=strlen(a+1);
    m=strlen(b+1);
    for(i=1;i<=n;i++)
    {
        for(j=1;j<=m;j++)
        {

        }
    }
    cout << dp[n][m] << "\n";
    return 0;
}
