#include <bits/stdc++.h>

using namespace std;
long long a[100010],b[100010];
char c;

int main()
{
    cin.tie(0)->sync_with_stdio(0);
    long long i,j,n,h,k;
    cin >> n >> h >> w;
    for(i=1;i<=n;i++)
    {
        cin >> a[i];
    }

    return 0;
}
